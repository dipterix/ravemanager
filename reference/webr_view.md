# Helper function to view `'htmlwidgets'` in 'WebAssembly'

Helper function to view `'htmlwidgets'` in 'WebAssembly'

## Usage

``` r
webr_view(widget)
```

## Arguments

- widget:

  `'htmlwidgets'` widget

## Value

nothing
